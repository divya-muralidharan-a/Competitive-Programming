#include <iostream>
using namespace std;

int main() {
	int x,y,z,max,min;
	cin>>x>>y>>z;
	if(x>y && x>z)
	max=x;
	else if(y>x && y>z)
	max=y;
	else
	max=z;
	if(x<y && x<z)
	min=x;
	else if(y<z && y<x)
	min=y;
	else 
	min=z;
	cout<<max-min;
	return 0;
}
